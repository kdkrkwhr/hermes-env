# .env.example — 새 PC에서 .env.local로 복사 후 값 채우기
# 이 파일은 Git에 커밋 가능 (값이 아니라 목록만)
# 실제 값이 들어간 .env.local은 절대 커밋 금지

# Notion integration 토큰 (https://notion.so/my-integrations)
NOTION_TOKEN=

# Hermes가 읽을 공통 시크릿 경로 기준
# E2E_ROOT는 bootstrap 0단계에서 결정
